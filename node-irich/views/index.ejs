<!DOCTYPE html>
<html>

<head>
  <title><%= title %></title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>

<body>
  <div class="cover-container d-flex w-100 h-100 p-3 mx-auto flex-column">
    <header class="masthead mb-auto">
      <div class="inner">
        <h3 class="masthead-brand"><%= title %></h3>
        <nav class="nav nav-masthead justify-content-center">
          <a class="nav-link active" href="/accounts/create">新增記帳</a>
        </nav>
      </div>
    </header>

    <main role="main" class="inner cover">
      <h1 class="cover-heading">Welcome to <%= title %></h1>
      <p class="lead"></p>
      <table class="table table-striped">
        <thead>
          <tr>
            <td>消費名稱</td>
            <td>消費蕾型</td>
            <td>消費價格</td>
            <td>更新</td>
            <td>刪除</td>
          </tr>
        </thead>
        <tbody>
          <% for(var i = 0; i < accounts.length; i++) { %>
          <tr>
            <td>
              <a href="/accounts/<%= accounts.id %>">
                <%= accounts[i].title %>
              </a>
            </td>
            <td><%= accounts[i].type %></td>
            <td><%= accounts[i].cost %></td>
            <td>
              <a href="/accounts/<%= accounts.id %>/update" class="btn btn-primary">
                更新
              </a>
            </td>
            <td>
              <form method="POST" action="/accounts/<%= accounts.id %>/delete">
                <button class="btn btn-danger">刪除</button>
              </form>
            </td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </main>

    <footer class="text-center mt-auto">
      <div class="inner">
        <p>Node.js iRich的練習...</p>
      </div>
    </footer>
  </div>
</body>

</html>